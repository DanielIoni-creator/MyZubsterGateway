# backend/Dockerfile.hardhat
FROM node:20-alpine

WORKDIR /app

# Installa dipendenze
COPY package*.json ./
RUN npm ci

# Copia il codice
COPY . .

# Esponi la porta
EXPOSE 8545

# Avvia Hardhat node
CMD ["npx", "hardhat", "node", "--hostname", "0.0.0.0", "--port", "8545"]