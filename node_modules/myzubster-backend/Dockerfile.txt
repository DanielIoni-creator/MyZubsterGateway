# backend/Dockerfile
FROM node:20-alpine

WORKDIR /app

# Installa dipendenze
COPY package*.json ./
RUN npm ci --only=production

# Copia il codice
COPY . .

# Esponi la porta
EXPOSE 3000

# Avvia il server
CMD ["npm", "start"]