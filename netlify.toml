[build]
  command = "npm install && npm run build || echo 'No build script, using static'"
  publish = "frontend"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
